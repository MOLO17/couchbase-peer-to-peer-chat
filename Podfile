# Uncomment the next line to define a global platform for your project
platform :ios, '13.2'

target 'CouchbaseTest' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CouchbaseTest
 # pod 'CouchbaseLite-Swift', '~> 2.7.0'
  pod 'CouchbaseLite-Swift-Enterprise', '~> 2.7.0'
  pod 'TinyConstraints', '~> 4.0.1'

  target 'CouchbaseTestTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
